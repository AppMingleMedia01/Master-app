import 'package:flutter/material.dart';

class SubjectiveExamsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exam Notes'),
      ),
      body: Center(
        child: Text('This is the Exam Notes screen'),
      ),
    );
  }
}